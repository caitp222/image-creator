import React from 'react';

const ImageDisplayHeader = (props) => {
  return(
    <nav>
      <div className="nav-wrapper">
        <a href="#" className="brand-logo center">Image Display</a>
      </div>
    </nav>
  )
}

export default ImageDisplayHeader;
